# Vnollx 在线评测系统 - 系统架构文档

## 1. 文档说明

本文档描述 Vnollx Online Judge 的**系统架构**，包括整体架构、技术选型、分层设计、数据流、AI 代理架构、WebSocket、安全与扩展要点。

---

## 2. 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            客户端 (浏览器)                                │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │ HTTPS / WSS
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    前端 (React 19 + TypeScript + Vite)                    │
│               TailwindCSS / 手写业务组件 / Monaco Editor                  │
│   SPA 路由、REST 调用 (/api/v1)、WebSocket (/ws/judge, /ws/ai, ...)      │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
          ┌───────────────────────┼───────────────────────┐
          │ REST API              │ WebSocket             │
          ▼                       ▼                       │
┌─────────────────────┐  ┌─────────────────────┐         │
│   Nginx (可选)       │  │   Nginx 反向代理     │         │
│   静态资源 + 反向代理 │  │   /api/v1 → 8080   │         │
│   前端路由 fallback  │  │   /ws/*   → 8080   │         │
└──────────┬──────────┘  └──────────┬──────────┘         │
           │                        │                     │
           └────────────────────────┼─────────────────────┘
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       后端 (Spring Boot 3.2 / Java 21)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │ Controller  │  │  Service    │  │   Mapper    │  │ WebSocket       │  │
│  │  (REST)     │  │  (业务逻辑)  │  │ (MyBatis+) │  │ Handler         │  │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └────────┬────────┘  │
│         │                │                │                   │          │
│  ┌──────┴────────────────┴────────────────┴───────────────────┴──────┐   │
│  │ Filter / AOP：JWT 鉴权、权限校验 (@RequirePermission)、XSS 过滤   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────┬──────────────┬──────────────┬──────────────┬──────────────┬───────┘
      │              │              │              │              │
      ▼              ▼              ▼              ▼              ▼
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐
│  MySQL   │  │  Redis   │  │ RabbitMQ │  │  MinIO   │  │  AI 代理 (Python) │
│ 主库/从库 │  │ 缓存/会话 │  │ 评测队列  │  │ 对象存储  │  │  FastAPI / 流式   │
└──────────┘  └──────────┘  └────┬─────┘  └──────────┘  └────────┬─────────┘
                                  │                               │
                                  ▼                               │
                    ┌──────────────┐ ┌──────────────┐             │
                    │ Go-Judge A   │ │ Go-Judge B   │             │
                    │ capacity=12  │ │ capacity=4   │             │
                    └──────────────┘ └──────────────┘             │
                                                                  │
                         ┌───────────────────────────────────────┘
                         ▼
                ┌─────────────────┐
                │  MySQL (OJ 库)  │  ← 代理只读查询（用户/题目/提交等）
                └─────────────────┘
```

---

## 3. 技术栈

### 3.1 后端

| 技术 | 版本/说明 |
|------|------------|
| Java | 21（含虚拟线程） |
| Spring Boot | 3.2.x |
| MyBatis-Plus | ORM、分页、条件构造 |
| MySQL | 8.0，主从复制、读写分离 |
| Druid | 数据源、主从数据源切换 |
| Redis | 缓存、权限缓存、分布式锁等 |
| RabbitMQ | 评测任务队列、异步解耦 |
| MinIO | 对象存储（头像、题目数据等） |
| Go-Judge | 独立评测服务，支持多端点 capacity 限流与熔断 |
| Spring WebSocket | 评测结果、通知、私信、AI 流式 |

### 3.2 前端

| 技术 | 说明 |
|------|------|
| React | 19.x |
| TypeScript | 5.x |
| Vite | 7.x，构建与 dev server |
| 手写业务组件 | 管理后台与业务页面组件 |
| TailwindCSS | 工具类 CSS |
| React Router | 6.x，SPA 路由 |
| Axios | HTTP 客户端，统一 baseURL /api/v1 |
| Monaco Editor | 代码编辑 |
| React-Markdown / KaTeX 等 | 题解、AI 回复渲染 |

### 3.3 AI 代理（独立服务）

| 技术 | 说明 |
|------|------|
| Python | 3.x |
| FastAPI | HTTP/SSE 接口 |
| 多厂商 SDK/API | Gemini、Kimi、MiniMax、Mistral、DeepSeek、通义千问、智谱等 |
| MySQL 只读 | 工具调用时查 OJ 库（用户、题目、提交等） |

---

## 4. 后端分层与模块

### 4.1 包结构概览

```
com.example.vnollxonlinejudge/
├── controller/          # REST 控制器（按资源划分）
├── service/             # 业务逻辑
│   └── serviceImpl/
├── mapper/              # MyBatis-Plus Mapper
├── model/               # 实体、DTO、VO、枚举等
│   ├── entity/
│   ├── dto/
│   ├── vo/
│   └── base/             # 如 PermissionCode
├── config/              # 数据源、Redis、RabbitMQ、WebSocket、监控等
├── filter/              # JWT、XSS 等过滤器
├── aspect/              # AOP（如权限校验）
├── websocket/           # 各 WebSocket Handler
├── consumer/            # RabbitMQ 消费者（评测）
├── producer/            # 消息生产者
├── exception/           # 全局异常处理
├── utils/               # 工具类
└── annotation/          # 自定义注解（如 @RequirePermission）
```

### 4.2 控制器与路由前缀

| 模块 | 前缀 | 说明 |
|------|------|------|
| 用户 | `/api/v1/user` | 登录、注册、资料、已通过题目、学习统计等 |
| 题目 | `/api/v1/problem` | 题目列表、详情、标签、数量 |
| 评测 | `/api/v1/judge` | 提交、测试运行 |
| 提交 | `/api/v1/submission` | 提交列表、详情、数量 |
| 比赛 | `/api/v1/competition` | 比赛列表、详情、排名、题目、报名等 |
| 练习 | `/api/v1/practice` | 练习列表、详情、题目 |
| 题解 | `/api/v1/solve` | 题解 CRUD、列表、详情 |
| 标签 | `/api/v1/tag` | 标签列表 |
| 评论 | `/api/v1/comment` | 评论发布、列表 |
| 好友 | `/api/v1/friend` | 好友搜索、请求、列表、私信 |
| 通知 | `/api/v1/notification` | 通知列表、未读、详情 |
| AI | `/api/v1/ai` | 模型列表、会话、历史、SSE 流式聊天 |
| 邮件 | `/api/v1/email` | 发送验证码等 |
| 管理端 | `/api/v1/admin/*` | 用户/题目/比赛/练习/题解/通知/统计/权限/AI 模型等 |

### 4.3 WebSocket 端点

| 路径 | Handler | 用途 |
|------|---------|------|
| `/ws/judge` | JudgeWebSocketHandler | 评测结果实时推送给对应用户 |
| `/ws/notification` | NotificationWebSocketHandler | 系统通知实时推送 |
| `/ws/message` | MessageWebSocketHandler | 好友私信、好友请求通知 |
| `/ws/ai` | AiWebSocketHandler | AI 流式对话（与 Python 代理配合） |

---

## 5. 数据流与关键流程

### 5.1 提交评测流程

```
用户提交代码
    → JudgeController.submit
    → 生成 snowflakeId
    → 写入 Submission(status=等待评测, queueAhead=前方等待数快照)
    → 发送 RabbitMQ 消息
    → 前端轮询或更常见：建立 /ws/judge 连接
    → JudgeConsumer 消费消息
    → GoJudgeRouter 按端点 capacity 获取一个 Go-Judge 许可
    → 绑定该端点完成编译、运行、清理等全部评测步骤
    → 更新 Submission 状态与结果
    → JudgeWebSocketHandler 按 uid 推送结果给前端
```

队列位置展示采用数据库口径：

- 入库前统计当前 `status='等待评测'` 的提交数，写入 `submission.queue_ahead`，作为提交响应和单条详情的快照值。
- 提交列表查询时对仍处于 `等待评测` 的行进行懒计算：取所有等待中提交的 `snow_flake_id` 升序列表，通过二分查找得到当前提交前方还有多少位，并覆盖返回给前端。
- 非 `等待评测` 状态的提交在列表中不返回 `queueAhead`，避免展示已过期的入库快照。

Go-Judge 调度采用容量限流：

- `go-judge.endpoints[]` 可配置多个评测端点，每个端点包含 `url` 与 `capacity`。
- 每个端点由一个信号量控制最大同时评测数，例如 3G 节点 `capacity=12`、1G 节点 `capacity=4`。
- 一次评测绑定同一个端点执行，避免编译产物 `fileId` 在不同 Go-Judge 机器之间不一致。
- RabbitMQ 消费者并发建议与所有端点 capacity 总和一致，超过 capacity 的任务留在 RabbitMQ 中等待。

题目判题模式：

- **标准题 `standard`**：测试数据 zip 中要求每个测试点同时存在 `.in` 与 `.out`，用户程序输出与标准输出进行比对。
- **构造题 `special`**：测试数据只要求 `.in`，管理员需上传 C++ Checker；评测时先编译用户程序和 Checker，再将输入与选手输出交给 Checker 判断。
- **浮点误差**：标准题输出比对支持数值 token 的容差判断；非数值 token 仍按文本 token 精确比较。

比赛参赛与防作弊：

- 比赛通过 `participantType` 区分个人赛 `INDIVIDUAL` 与团队赛 `TEAM`。
- 团队赛提交时后端根据比赛和当前用户解析所属队伍，排行榜按队伍维度展示队伍名称。
- 比赛通过 `antiCheatMode` 区分普通模式 `NORMAL` 与严格模式 `STRICT`。
- 严格模式下，前端在比赛做题页采集切屏、失焦、退出全屏、粘贴等网页可检测行为，后端记录事件、累计风险分并提供管理端人工复核。
- 防作弊只提供证据链和风险等级，不直接自动判定作弊。

### 5.2 读写分离

- **写**：走 master 数据源（提交、用户信息变更、题目/比赛管理等）。
- **读**：部分查询走 slave（如题目列表、提交列表、排名等），由动态数据源 + 注解或策略切换。

### 5.3 AI 对话流程（含 Python 代理）

```
前端 → /ws/ai 发送 JSON（type: chat, message, sessionId, modelId）
    → AiWebSocketHandler 接收
    → AiService 加载历史、拼装请求
    → ProxyAiStreamingClient 请求 Python 代理
    → Python: /v1/chat/stream/tools（SSE）
    → 代理调用对应 AI 厂商，可选执行 OjTools（查 MySQL）
    → 流式事件回传 Java
    → Java 转 WebSocket 推给前端
    → 对话落库（AiChatRecord 等）
```

详见 [AI代理架构.md](./AI代理架构.md)。

---

## 6. AI 代理架构（概要）

- **职责分离**：Java 负责会话、历史、鉴权；Python 负责模型调用、Function Calling、OjTools 执行。
- **协议**：Java 通过 HTTP/SSE 调用 Python `/v1/chat/stream/tools`，传入 api_key、model、messages、current_user_id 等；Python 返回 meta/content/thinking/error/done 等事件。
- **工具**：OjTools 在 Python 内直接查 MySQL（用户、题目、提交、比赛、练习等），供 AI 回答“我通过了多少题”“某题难度”等问题。
- **部署**：Python 代理可单独部署，支持国内/海外双地址配置（如 application.yml 中 domestic/overseas）。

---

## 7. 安全设计

- **认证**：JWT 放在 Header 或 Cookie，Filter 解析后写入上下文；未登录访问受保护接口返回 401。
- **权限**：RBAC，接口使用 @RequirePermission(PermissionCode.xxx)，AOP 根据当前用户角色查权限（含 Redis 缓存）。
- **输入**：XSS 过滤（如 XssFilter 对部分接口做请求体过滤）；SQL 使用参数化（MyBatis）。
- **密码**：BCrypt 存储；敏感接口可考虑限流。

---

## 8. 前端架构要点

- **路由**：React Router，/login、/register、/admin/*、/* 主布局；主布局内 /problems、/problem/:id、/submissions、/competitions、/practice、/user/:userId、/notifications 等。
- **API**：axios 实例 baseURL 为 `/api/v1`，请求拦截器附加 Token。
- **权限**：从后端拉取当前用户权限列表，前端用 PermissionCode 控制菜单与按钮（如 AI 对话、管理后台入口）。
- **WebSocket**：按需连接 /ws/judge、/ws/ai、/ws/notification、/ws/message，按业务订阅推送。
- **比赛防作弊**：比赛题目页在严格模式下启用行为采集 hook，普通模式和非比赛题目页不采集。
- **管理后台**：支持题目判题模式/Checker 上传、比赛参赛模式/防作弊模式配置、防作弊风险审查与 CSV 导出。

---

## 9. 部署架构（逻辑）

- **前端**：静态构建（Vite build），由 Nginx 托管或与后端同域；Nginx 将 /api/v1 与 /ws 反向代理到后端 8080。
- **后端**：单实例或多实例无状态部署，会话不依赖本地内存（JWT + Redis 可选）。
- **MySQL**：主从复制，应用配置主从数据源。
- **Redis**：单机或集群，缓存与锁。
- **RabbitMQ**：评测队列，消费者可水平扩展。
- **Go-Judge**：独立评测沙箱，支持多个端点按 capacity 限流调度；跨公网部署时需限制 5050 端口访问来源。
- **AI 代理**：独立 Python 服务，**建议国内 / 海外双机部署**（国内代理 + 海外代理），后台为每个模型配置代理类型，Java 按类型选择对应 URL 访问。

---

## 10. 扩展与演进

- **水平扩展**：后端、评测消费者、AI 代理均可多实例；需保证 RabbitMQ 消费与 WebSocket 会话亲和性或广播方案。
- **新语言**：在 Go-Judge 与题目配置中增加语言类型即可。
- **新 AI 厂商**：在 Python 代理中新增 Provider，Java 侧仅配置 model 与 proxy 地址。
- **新功能**：按资源增加 Controller/Service/Mapper，权限表与 PermissionCode 同步扩展。

---

## 11. 相关文档

- [需求文档](./需求文档.md)
- [AI 代理架构](./AI代理架构.md)
- [部署文档](./部署文档.md)
